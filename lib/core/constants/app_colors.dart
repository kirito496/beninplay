import 'package:flutter/material.dart';

class AppColors {
  // Couleurs principales BeninPlay
  static const Color primary = Color(0xFF00C853);      // Vert Bénin
  static const Color primaryDark = Color(0xFF009624);
  static const Color accent = Color(0xFFFFD600);       // Jaune

  // Zone Normal
  static const Color normalBg = Color(0xFF000000);
  static const Color normalSurface = Color(0xFF1A1A1A);

  // Zone Dark (+18)
  static const Color darkBg = Color(0xFF0D0010);       // Violet très sombre
  static const Color darkPrimary = Color(0xFF9C27B0);  // Violet
  static const Color darkAccent = Color(0xFFE91E63);   // Rose

  // Textes
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFFB0B0B0);
  static const Color textHint = Color(0xFF666666);

  // UI
  static const Color divider = Color(0xFF2A2A2A);
  static const Color error = Color(0xFFFF5252);
  static const Color success = Color(0xFF00C853);
  static const Color warning = Color(0xFFFFD600);

  // Mobile Money
  static const Color mtnYellow = Color(0xFFFFCC00);
  static const Color moovBlue = Color(0xFF0066CC);
}
